function format(){

}

function run(fn, options){
  const task = typeof fn.default ===
  return task(options).then(() => {

  });
}

if(process.mainModule.children.length === 0 && process.argv.length > 2){

}

export default run;
